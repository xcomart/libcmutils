{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
//            "command": "mkdir -p ../libcmutils-build;cd ../libcmutils-build;qmake ../libcmutils/libcmutils.pro;make"
            "command": "cd ../libcmutils-build;make"
        },
        {
            "label": "cmake",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/../libcmutils-build"
            },
            "command": "rm -r ${workspaceRoot}/../libcmutils-build;mkdir -p ${workspaceRoot}/../libcmutils-build && cd ${workspaceRoot}/../libcmutils-build && cmake ${workspaceRoot}"
        }
    ]
}